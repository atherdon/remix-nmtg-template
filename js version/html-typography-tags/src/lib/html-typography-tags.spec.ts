import { htmlTypographyTags } from './html-typography-tags';

describe('htmlTypographyTags', () => {
  it('should work', () => {
    expect(htmlTypographyTags()).toEqual('html-typography-tags');
  });
});
